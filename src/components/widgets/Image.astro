---
interface Image {
  src: string;
  alt?: string;
}
interface Props {
  image: Image;
  class?: string;
}

const { class: className, image, ...rest } = Astro.props;
const { src, alt } = image;
---

<img src={src} alt={alt} class={className} {...rest} />
